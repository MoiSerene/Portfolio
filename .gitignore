# macOS
.DS_Store
**/.DS_Store

# Local workspace data (project memory, session data — not for public repo)
.workbuddy/

# Large / source-only assets (keep the optimized versions only)
# hero.png is NOW referenced by about.html — must be tracked!
# hero-bw.png = obsolete PIL-processed grayscale variant (no longer used)
# IMG_5831.JPG = previous hero portrait (unreferenced)
images/hero-bw.png
images/IMG_5831.JPG
